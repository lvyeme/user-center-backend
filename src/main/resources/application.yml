spring:
  application:
    name: user-center
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/idol
    username: root
    password: lvye
server:
  port: 8080
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false


#下面这些内容是为了让MyBatis映射
#指定Mybatis的Mapper文件
#mybatis.mapper-locations=classpath:mappers/*xml
#指定Mybatis的实体目录
#mybatis.type-aliases-package=com.lvye.usercenterbackend.mybatis.entity
# 应用服务 WEB 访问端口
#server.port=8080

